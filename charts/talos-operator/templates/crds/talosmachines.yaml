{{- if .Values.installCRDs }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: talosmachines.talos.alperen.cloud
spec:
  group: talos.alperen.cloud
  names:
    kind: TalosMachine
    listKind: TalosMachineList
    plural: talosmachines
    shortNames:
    - tm
    singular: talosmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TalosMachine is the Schema for the talosmachines API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TalosMachine.
            properties:
              configRef:
                description: configRef is a reference to a ConfigMap containing the
                  Talos cluster configuration.
                properties:
                  key:
                    description: The key to select.
                    type: string
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  optional:
                    description: Specify whether the ConfigMap or its key must be
                      defined
                    type: boolean
                required:
                - key
                type: object
                x-kubernetes-map-type: atomic
              controlPlaneRef:
                description: controlPlaneRef is a reference to the TalosControlPlane
                  this machine belongs to.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              endpoint:
                description: endpoint is the Talos API endpoint for this machine.
                type: string
              machineSpec:
                description: machineSpec is the machine specification for this TalosMachine.
                properties:
                  additionalConfig:
                    description: |-
                      additionalConfig is a list of additional Talos configuration documents to append to the
                      generated config, each separated by "---". Entries are appended in order: global first,
                      then machine-specific.
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  airGap:
                    default: false
                    description: airGap indicates whether the machine is in an air-gapped
                      environment.
                    type: boolean
                  allowSchedulingOnControlPlanes:
                    default: false
                    description: allowSchedulingOnControlPlanes indicates whether
                      to allow scheduling workloads on control plane nodes.
                    type: boolean
                  configPatches:
                    description: |-
                      configPatches is a list of strategic merge patches applied to the generated Talos machine config.
                      Unlike additionalConfig (which appends a separate YAML document), each patch is merged into
                      the main machine config, allowing you to override or extend any field (e.g. machine.network).
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  image:
                    description: image is the Talos image to use for this machine.
                    type: string
                  imageCache:
                    default: false
                    description: imageCache indicates whether to enable local image
                      caching on the machine.
                    type: boolean
                  installDisk:
                    description: installDisk is the disk to use for installing Talos
                      on the control plane machines.
                    pattern: ^/dev/(sd[a-z][0-9]*|vd[a-z][0-9]*|nvme[0-9]+n[0-9]+(p[0-9]+)?)$
                    type: string
                  meta:
                    description: meta is the meta partition used by Talos.
                    properties:
                      dnsServers:
                        description: dnsServers is a list of DNS servers for the Talos
                          machines.
                        items:
                          type: string
                        type: array
                      gateway:
                        description: gateway is the gateway for the Talos machines.
                        type: string
                      hostname:
                        description: hostname is the hostname for the Talos machines.
                        type: string
                      interface:
                        description: interface is the network interface name for Talos
                          machines.
                        type: string
                      subnet:
                        description: subnet is the subnet prefix length for the Talos
                          machines.
                        type: integer
                    type: object
                  registries:
                    description: registries is the path to a custom registries configuration
                      file.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  wipe:
                    default: false
                    description: wipe indicates whether to wipe the disk before installation.
                    type: boolean
                type: object
              preserveMachineOnDeletion:
                default: false
                description: preserveMachineOnDeletion indicates whether the machine
                  should be preserved (not reset) when deleting this Kubernetes resource
                type: boolean
              version:
                description: version is the desired version of Talos to run on this
                  machine.
                type: string
              workerRef:
                description: workerRef is a reference to the TalosWorker this machine
                  belongs to.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - endpoint
            - version
            type: object
          status:
            description: status defines the observed state of TalosMachine.
            properties:
              conditions:
                description: conditions represent the latest available observations
                  of a TalosMachine's current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              config:
                description: config is the base64 encoded Talos configuration.
                type: string
              imported:
                description: imported is only valid when ReconcileMode is 'import'
                  and indicates whether the Talos machine has been imported.
                type: boolean
              observedVersion:
                description: observedVersion is the version of Talos running on this
                  machine.
                type: string
              state:
                description: state is the current state of the machine (e.g., "Ready",
                  "Provisioning", "Failed").
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
