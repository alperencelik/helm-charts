{{- if .Values.installCRDs }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: talosetcdbackupschedules.talos.alperen.cloud
spec:
  group: talos.alperen.cloud
  names:
    kind: TalosEtcdBackupSchedule
    listKind: TalosEtcdBackupScheduleList
    plural: talosetcdbackupschedules
    shortNames:
    - tebs
    singular: talosetcdbackupschedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.schedule
      name: Schedule
      type: string
    - jsonPath: .status.lastSuccessfulBackupTime
      name: Last Backup
      type: date
    - jsonPath: .status.nextScheduleTime
      name: Next Backup
      type: date
    - jsonPath: .spec.paused
      name: Paused
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TalosEtcdBackupSchedule is the Schema for the talosetcdbackupschedules
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TalosEtcdBackupSchedule
            properties:
              backupTemplate:
                description: backupTemplate is the template for creating TalosEtcdBackup
                  resources.
                properties:
                  spec:
                    description: spec is the specification of the TalosEtcdBackup
                      to be created.
                    properties:
                      backupStorage:
                        description: backupStorage specifies where to store the etcd
                          backup.
                        properties:
                          s3:
                            description: s3 specifies the S3-compatible storage configuration
                              for the etcd backup.
                            properties:
                              accessKeyID:
                                description: accessKeyID is the access key ID for
                                  the S3 bucket.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              bucket:
                                description: bucket is the name of the S3 bucket to
                                  store the etcd backup.
                                type: string
                              endpoint:
                                description: endpoint is the S3 service endpoint (optional,
                                  for custom S3-compatible services).
                                type: string
                              insecureSkipTLSVerify:
                                default: false
                                description: insecureSkipTLSVerify skips TLS verification
                                  for the S3 endpoint (optional).
                                type: boolean
                              region:
                                description: region is the AWS region where the S3
                                  bucket is located.
                                type: string
                              secretAccessKey:
                                description: secretAccessKey is the secret access
                                  key for the S3 bucket.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            required:
                            - accessKeyID
                            - bucket
                            - region
                            - secretAccessKey
                            type: object
                        required:
                        - s3
                        type: object
                      talosControlPlaneRef:
                        description: talosControlPlaneRef is a reference to the TalosControlPlane
                          this backup is associated with.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    required:
                    - backupStorage
                    - talosControlPlaneRef
                    type: object
                required:
                - spec
                type: object
              paused:
                default: false
                description: paused can be set to true to pause the backup schedule.
                type: boolean
              retention:
                default: 5
                description: |-
                  retention specifies how many successful backups to keep.
                  Older backups will be automatically deleted
                format: int32
                minimum: 1
                type: integer
              schedule:
                description: |-
                  schedule is a cron expression defining when to run backups.
                  For example: "0 2 * * *" for daily backups at 2 AM
                minLength: 1
                type: string
            required:
            - backupTemplate
            - schedule
            type: object
          status:
            description: status defines the observed state of TalosEtcdBackupSchedule
            properties:
              activeBackups:
                description: activeBackups is the list of currently active backup
                  jobs.
                items:
                  type: string
                type: array
              conditions:
                description: conditions represent the current state of the TalosEtcdBackupSchedule
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastScheduleTime:
                description: lastScheduleTime is the last time a backup was scheduled.
                format: date-time
                type: string
              lastSuccessfulBackupTime:
                description: lastSuccessfulBackupTime is the last time a backup completed
                  successfully.
                format: date-time
                type: string
              nextScheduleTime:
                description: nextScheduleTime is the next time a backup will be scheduled.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
